One-line: the full-bleed GLØD band — the only saturated surface on a page, used once as a breaker.

```jsx
<CtaBand title="Klar til at komme i gang?" cta="Bliv ringet op" href="/kontakt" />
```

- Always full-bleed, never inset or rounded. The button inside is `variant="onbrand"` (white).
- The section right after a band drops its top hairline — the band already separates them.
